# Example .env file for acft claude configuration
# Copy to .env and customize as needed

# Prelaunch hook - runs before launching Claude
# Useful for environment setup, validation, or pre-flight checks
# This only sets the value if not already set in the environment
: "${ACFT_CLAUDE_PRELAUNCH_HOOK:=echo 'Launching Claude...'}"

# To always override environment values, use direct assignment instead:
# ACFT_CLAUDE_PRELAUNCH_HOOK="echo 'Launching Claude...'"
